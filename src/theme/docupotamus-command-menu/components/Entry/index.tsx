import Box from '@mui/material/Box';
import * as React from 'react';

export default function Entry(): JSX.Element {
    return (
        <Box sx={{ position: 'fixed', bottom: 0, width: '100px', height: '100px', backgroundColor: 'red' }} >
            no
        </Box>
    );
}